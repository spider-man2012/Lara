rpm -qa | grep mysql

service mysqld start

mysqladmin -u root password 'admin123'

mysql -u root -p

select user,host,password from mysql.user;

create user 'mysqlmega'@'localhost' identified by 'admin123';

grant all privileges on *.* to 'mysqlmega'@'localhost' with grant option;

exit

mysql -u mysqlmega -p

exit
