rpm -qa | grep nfs
ifconfig
cd /root
pwd
mkdir servernfs
cd servernfs
cat>newfile
Hi! This is TYCS


vi /etc/exports
/root/servernfs *(rw,sync)

service nfs start

service nfs restart

service iptables stop

service iptables status

service vsftpd stop

service vsftpd status

service vsftpd status

chmod -R 777 /root/servernfs/


Client Side:
cd /root/
ls
mkdir clientnfs
mount -t nfs 192.168.122.1:/root/servernfs/ /root/clientnfs/
cd clientnfs/ 
ls
